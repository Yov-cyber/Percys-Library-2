<Application x:Class="ComicReader.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:ComicReader.Converters"
             xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <Application.Resources>
        <ResourceDictionary>
            <!--
              Sistema de diseño unificado (Phase 1).
              Sólo dos diccionarios: tokens (paleta, tipografía, escalas) y components
              (estilos canónicos de botón / card / input / slider / progress / list).
              Cualquier brush o estilo legacy queda como alias dentro de esos dos archivos.
              No agregues más MergedDictionaries aquí: si necesitas un nuevo recurso,
              añádelo a Tokens.xaml o Components.xaml.
            -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Tokens.xaml" />
                <ResourceDictionary Source="Themes/Components.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Render hints -->
            <sys:Boolean x:Key="UseBitmapScalingModeHighQuality">True</sys:Boolean>
            <sys:Boolean x:Key="PreferHighQualityScaling">True</sys:Boolean>

            <!-- Default Window style: superficie base + tipografía única -->
            <Style TargetType="Window">
                <Setter Property="Background" Value="{DynamicResource WindowBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
                <Setter Property="FontSize" Value="{DynamicResource FontSize.Body}"/>
            </Style>

            <!-- Convertidores -->
            <converters:NullToCollapsedConverter x:Key="NullToCollapsedConverter"/>
            <converters:NullToVisibleConverter x:Key="NullToVisibleConverter"/>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <converters:IsComicViewVisibleConverter x:Key="IsComicViewVisibleConverter"/>
            <converters:StringToBrushConverter x:Key="StringToBrushConverter"/>
        </ResourceDictionary>
    </Application.Resources>
</Application>
