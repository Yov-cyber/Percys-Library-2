<UserControl x:Class="ComicReader.Views.ContinuousComicView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:local="clr-namespace:ComicReader.Views"
			 mc:Ignorable="d"
			 d:DesignHeight="500" d:DesignWidth="800">
	<UserControl.Resources>
		<local:ZoomedWidthConverter x:Key="ZoomedWidthConverter" />
	</UserControl.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
			<Grid Grid.Row="0" Margin="0,0,0,0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			<!-- Contenido continuo: eliminada columna de miniaturas y panel flotante -->
				<ListBox x:Name="PagesList" ItemsSource="{Binding Pages}" BorderThickness="0" Background="Transparent"
						 ScrollViewer.VerticalScrollBarVisibility="Auto"
						 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
						 ScrollViewer.CanContentScroll="False"
						 VirtualizingStackPanel.IsVirtualizing="True"
						 VirtualizingStackPanel.VirtualizationMode="Recycling">
					<ListBox.ItemsPanel>
						<ItemsPanelTemplate>
							<VirtualizingStackPanel Orientation="Vertical"/>
						</ItemsPanelTemplate>
					</ListBox.ItemsPanel>
					<ListBox.ItemTemplate>
						<DataTemplate>
							<Border Margin="0,0,0,16" Padding="0" Background="Transparent" SnapsToDevicePixels="True">
								<Grid>
									<!-- Thumbnail blur placeholder (se muestra primero si existe)
									     y la imagen principal (full) aparece encima y hace fade-in -->
									<Grid>
										<Image x:Name="ThumbImage" Stretch="Uniform" Opacity="1" HorizontalAlignment="Center" IsHitTestVisible="False"
											   SnapsToDevicePixels="True"
											   RenderOptions.BitmapScalingMode="HighQuality"
											   RenderOptions.EdgeMode="Aliased">
											<Image.Source>
												<Binding Path="Thumbnail" />
											</Image.Source>
											<Image.Effect>
												<BlurEffect Radius="8"/>
											</Image.Effect>
											<Image.Width>
												<MultiBinding Converter="{StaticResource ZoomedWidthConverter}">
													<Binding ElementName="PagesList" Path="ActualWidth" />
													<Binding Path="DataContext.Zoom" RelativeSource="{RelativeSource AncestorType=UserControl}" />
												</MultiBinding>
											</Image.Width>
										</Image>

										<Image x:Name="PageImage" Stretch="Uniform" Loaded="Image_Loaded" Opacity="0" HorizontalAlignment="Center"
											   SnapsToDevicePixels="True"
											   RenderOptions.BitmapScalingMode="HighQuality"
											   RenderOptions.EdgeMode="Aliased">
											<Image.Source>
												<Binding Path="Image" NotifyOnTargetUpdated="True"/>
											</Image.Source>
											<Image.Width>
												<MultiBinding Converter="{StaticResource ZoomedWidthConverter}">
													<Binding ElementName="PagesList" Path="ActualWidth" />
													<Binding Path="DataContext.Zoom" RelativeSource="{RelativeSource AncestorType=UserControl}" />
												</MultiBinding>
											</Image.Width>
										</Image>
									</Grid>
								</Grid>
							</Border>
						</DataTemplate>
					</ListBox.ItemTemplate>
				</ListBox>
            
		</Grid>
	</Grid>
</UserControl>
