<UserControl x:Class="ComicReader.Views.FileExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400"
             Background="{DynamicResource PanelBackgroundBrush}">

    <UserControl.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FileListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>

        <Style x:Key="FileItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}"
                                Margin="{TemplateBinding Margin}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ItemHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ItemSelectedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Barra de navegación -->
        <Border Grid.Row="0" Background="{DynamicResource HeaderBackgroundBrush}" 
                CornerRadius="10,10,0,0" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="⬆" Style="{StaticResource ModernButtonStyle}"
                        Command="{Binding GoUpCommand}" ToolTip="Subir un nivel"/>
                
                <Button Grid.Column="1" Content="🔄" Style="{StaticResource ModernButtonStyle}"
                        Command="{Binding RefreshCommand}" ToolTip="Actualizar"/>

                <TextBox Grid.Column="2" Text="{Binding CurrentPath, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="10,0" Padding="8" FontSize="12"
                         Background="{DynamicResource InputBackgroundBrush}"
                         Foreground="{DynamicResource TextBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         VerticalAlignment="Center"/>

                <Button Grid.Column="3" Content="⭐" Style="{StaticResource ModernButtonStyle}"
                        Command="{Binding AddToFavoritesCommand}" ToolTip="Agregar a favoritos"/>
            </Grid>
        </Border>

        <!-- Búsqueda -->
        <Border Grid.Row="1" Background="{DynamicResource PanelBackgroundBrush}" Padding="10">
            <TextBox Text="{Binding SearchFilter, UpdateSourceTrigger=PropertyChanged}" 
                     Padding="8" FontSize="12"
                     Background="{DynamicResource InputBackgroundBrush}"
                     Foreground="{DynamicResource TextBrush}"
                     BorderBrush="{DynamicResource BorderBrush}">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <VisualBrush Stretch="None">
                                            <VisualBrush.Visual>
                                                <Label Content="Buscar archivos..." Foreground="Gray"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Border>

        <!-- Carpetas recientes -->
        <Expander Grid.Row="2" Header="Carpetas Recientes" 
                  Background="{DynamicResource PanelBackgroundBrush}"
                  Foreground="{DynamicResource TextBrush}"
                  Margin="5" IsExpanded="False">
            <ListBox ItemsSource="{Binding RecentFolders}" 
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MaxHeight="120">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" 
                                   Foreground="{DynamicResource TextBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   ToolTip="{Binding}"
                                   Cursor="Hand">
                            <TextBlock.InputBindings>
                                <MouseBinding Gesture="LeftClick" 
                                            Command="{Binding DataContext.GoToFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                            </TextBlock.InputBindings>
                        </TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Expander>

        <!-- Lista de archivos -->
        <ListBox Grid.Row="3" ItemsSource="{Binding CurrentItems}" 
                 Style="{StaticResource FileListBoxStyle}"
                 ItemContainerStyle="{StaticResource FileItemStyle}"
                 Background="{DynamicResource PanelBackgroundBrush}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Ícono -->
                        <Border Grid.Column="0" Width="32" Height="32" Margin="0,0,8,0">
                            <TextBlock Text="{Binding IsDirectory, Converter={StaticResource BoolToIconConverter}}" 
                                       FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Foreground="{DynamicResource AccentBrush}"/>
                        </Border>

                        <!-- Información del archivo -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" 
                                       Foreground="{DynamicResource TextBrush}"
                                       FontWeight="{Binding IsComicFile, Converter={StaticResource BoolToFontWeightConverter}}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding SizeText}" 
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       FontSize="10" Opacity="0.8"/>
                        </StackPanel>

                        <!-- Fecha de modificación -->
                        <TextBlock Grid.Column="2" Text="{Binding DateModified, StringFormat=dd/MM/yyyy}" 
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   FontSize="10" VerticalAlignment="Center" Margin="8,0,0,0"/>

                        <!-- Eventos de doble click -->
                        <Grid.InputBindings>
                            <MouseBinding Gesture="LeftDoubleClick" 
                                        Command="{Binding DataContext.ItemDoubleClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                        </Grid.InputBindings>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Panel de opciones -->
        <Border Grid.Row="4" Background="{DynamicResource HeaderBackgroundBrush}" 
                CornerRadius="0,0,10,10" Padding="10">
            <StackPanel Orientation="Horizontal">
                <CheckBox Content="Mostrar archivos ocultos" 
                          IsChecked="{Binding ShowHiddenFiles}"
                          Foreground="{DynamicResource TextBrush}"
                          Margin="0,0,20,0"/>
                
                <TextBlock Text="{Binding CurrentItems.Count, StringFormat='{}{0} elementos'}" 
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>