<Window x:Class="ComicReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:ComicReader.Views"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:models="clr-namespace:ComicReader.Models"
        xmlns:services="clr-namespace:ComicReader.Services"
        mc:Ignorable="d"
        Title="{Binding Title, RelativeSource={RelativeSource Self}}"
        Background="{DynamicResource WindowBackgroundBrush}"
    Icon="icono.ico"
    WindowStyle="None" AllowsTransparency="False" ResizeMode="CanResize"
    WindowState="Maximized"
    AllowDrop="True"
    Closing="MainWindow_Closing"
    StateChanged="Window_StateChanged">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40"
                            ResizeBorderThickness="6"
                            GlassFrameThickness="0"
                            CornerRadius="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style TargetType="Path" x:Key="NavIconStyle">
            <Setter Property="Fill" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Width" Value="14"/>
            <Setter Property="Height" Value="14"/>
            <Setter Property="Stretch" Value="Uniform"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barra de Título Personalizada (siempre visible) -->
    <Border Grid.Row="0" x:Name="CustomTitleBar" 
                Background="{DynamicResource HeaderBackgroundBrush}" 
                MouseLeftButtonDown="DragWindow_MouseLeftButtonDown"
                Height="40">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                    <TextBlock Text="📚 Percy's Library" 
                               Style="{DynamicResource AppTitleTextStyle}" 
                               FontSize="20" 
                               Foreground="{DynamicResource AccentColorBrush}" 
                               VerticalAlignment="Center"/>
                </StackPanel>

        <!-- Botones del encabezado removidos por solicitud -->

        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right"
                shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <Button Content="★" ToolTip="Favoritos" Click="ToggleFavorites_Click" Style="{StaticResource WindowControlButton}"
                shell:WindowChrome.IsHitTestVisibleInChrome="True" Margin="0,0,6,0"/>
            <Button Content="📖" ToolTip="Entrar al modo lectura" Click="EnterReadingMode_Click" Style="{StaticResource WindowControlButton}"
                shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
            <Button Content="⚙" ToolTip="Configuración" Click="OpenSettingsDialog_Click" Style="{StaticResource WindowControlButton}"
                shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
            <Button Content="─" Click="Minimize_Click" Style="{StaticResource WindowControlButton}"
                shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
            <Button x:Name="MaxRestoreButton"
                    Click="MaximizeRestore_Click"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
                        <!-- Segoe MDL2 Assets: Maximize = E922, Restore = E923 -->
                        <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
                        <Setter Property="Content" Value="&#xE922;"/>
                        <Setter Property="ToolTip" Value="Maximizar"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource AncestorType=Window}}" Value="Maximized">
                                <!-- Icono de restaurar (dos cuadros superpuestos) MDL2 = E923 -->
                                <Setter Property="Content" Value="&#xE923;"/>
                                <Setter Property="ToolTip" Value="Restaurar"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="✕" Click="Close_Click" Style="{StaticResource WindowControlButton}"
                shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Área de Contenido Principal -->
    <!-- Barra superior de controles del lector -->
    <Border Grid.Row="1" x:Name="ReaderTopBar"
        Panel.ZIndex="1000"
        IsEnabled="{Binding IsReaderViewActive, RelativeSource={RelativeSource AncestorType=Window}}">
        <Border.Style>
            <Style TargetType="Border" BasedOn="{StaticResource ReaderTopBarStyle}">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsReaderViewActive, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding IsHomeViewActive, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding IsSettingsViewActive, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        
        <Grid>
            <Rectangle Fill="{StaticResource ComicHalftoneBrush}" Opacity="0.35"/>
            <DockPanel>
            <!-- Izquierda: navegación -->
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                <Button ToolTip="Inicio" Click="HomeButton_Click" Style="{DynamicResource ReaderIconButtonStyle}" Margin="2,0">
                    <Path Style="{StaticResource NavIconStyle}" Data="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z" Fill="{DynamicResource TextBrush}"/>
                </Button>
                <Button ToolTip="Abrir cómic (Ctrl+O)" Click="OpenFile_Click" Style="{DynamicResource ReaderIconButtonStyle}" Margin="2,0">
                    <Path Style="{StaticResource NavIconStyle}" Data="M20 6h-8l-2-2H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V8c0-1.1-.9-2-2-2z" Fill="{DynamicResource TextBrush}"/>
                </Button>
                <Button x:Name="PrevButton" ToolTip="Anterior" Click="PrevPage_Click" Style="{DynamicResource ReaderIconButtonStyle}" Margin="2,0">
                    <!-- Contenido por defecto: flecha izquierda -->
                    <Path Style="{StaticResource NavIconStyle}" Data="M12,3 L4,12 L12,21" Stroke="{DynamicResource TextBrush}" StrokeThickness="2" StrokeEndLineCap="Round" StrokeStartLineCap="Round" StrokeLineJoin="Round"/>
                </Button>
                <Button x:Name="NextButton" ToolTip="Siguiente" Click="NextPage_Click" Style="{DynamicResource ReaderIconButtonStyle}" Margin="2,0">
                    <Path Style="{StaticResource NavIconStyle}" Data="M4,3 L12,12 L4,21" Stroke="{DynamicResource TextBrush}" StrokeThickness="2" StrokeEndLineCap="Round" StrokeStartLineCap="Round" StrokeLineJoin="Round"/>
                </Button>
            </StackPanel>
            <!-- Centro: indicador + slider -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Pág" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="PageIndicator" Text="— / —" Foreground="{DynamicResource TextBrush}" FontWeight="Bold" VerticalAlignment="Center"/>
                <!-- Barra de progreso del lector (colocada junto al indicador de página) -->
                <ProgressBar x:Name="ReaderProgressBar" Width="260" Height="12" Minimum="0" Maximum="100" Value="0" Margin="8,0,0,0" VerticalAlignment="Center" Style="{DynamicResource ReaderProgressBarStyle}" Visibility="Visible"/>
                <Slider x:Name="PageSlider" Width="220" Minimum="1" Maximum="1" Value="1" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="PageSlider_ValueChanged" Margin="12,0,0,0" Style="{DynamicResource ReaderSliderStyle}"/>
            </StackPanel>
            <!-- Derecha: zoom y utilidades -->
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="－" ToolTip="Zoom Out" Click="ZoomOut_Click" Style="{DynamicResource ReaderIconButtonStyle}" Margin="2,0"/>
                <Button Content="100%" ToolTip="Restablecer" Click="Zoom100_Click" Style="{DynamicResource ReaderIconButtonStyle}" Margin="2,0"/>
                <Button Content="＋" ToolTip="Zoom In" Click="ZoomIn_Click" Style="{DynamicResource ReaderIconButtonStyle}" Margin="2,0"/>
                <Separator Width="10" Background="Transparent"/>
                <Button ToolTip="Rotar" Click="Rotate_Click" Style="{DynamicResource ReaderIconButtonStyle}" Margin="2,0">
                    <Path Style="{StaticResource NavIconStyle}" Data="M12 6V2L8 6l4 4V8c2.8 0 5 2.2 5 5 0 1.1-.3 2.1-.9 3l1.5 1.5C19.4 16.1 20 14.1 20 12c0-4.4-3.6-8-8-8z" Fill="{DynamicResource TextBrush}"/>
                </Button>
                <Separator Width="10" Background="Transparent"/>
                <Button ToolTip="Miniaturas" Click="ToggleThumbnails_Click" Style="{DynamicResource ReaderIconButtonStyle}" Margin="2,0">
                    <Path Style="{StaticResource NavIconStyle}" Data="M3 6h18v2H3zM3 11h18v2H3zM3 16h18v2H3z" Fill="{DynamicResource TextBrush}"/>
                </Button>
                <Button ToolTip="Modo Nocturno" Click="ToggleNightMode_Click" Style="{DynamicResource ReaderIconButtonStyle}" Margin="2,0">
                    <Path Style="{StaticResource NavIconStyle}" Data="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z" Fill="{DynamicResource TextBrush}"/>
                </Button>
                <Button ToolTip="Pantalla Completa Total" Click="ImmersiveFullScreen_Click" Style="{DynamicResource ReaderIconButtonStyle}" Margin="2,0">
                    <Path Style="{StaticResource NavIconStyle}" Data="M7 14H5v5h5v-2H7v-3zm10 5h-3v2h5v-5h-2v3zM7 5h3V3H5v5h2V5zm10 0v3h2V3h-5v2h3z" Fill="{DynamicResource TextBrush}"/>
                </Button>
                <!-- Toggle lectura continua / paginada -->
                <ToggleButton x:Name="ToggleContinuousButton" ToolTip="Alternar modo de lectura" Click="ToggleContinuous_Click" Style="{DynamicResource ReaderToggleButtonStyle}" Margin="6,0">
                    <Path Style="{StaticResource NavIconStyle}" Data="M6 2h8l4 4v14H6z" Fill="{DynamicResource TextBrush}"/>
                </ToggleButton>
            </StackPanel>
            </DockPanel>
        </Grid>
    </Border>

    <!-- Contenido principal con panel de miniaturas -->
    <Grid Grid.Row="2" x:Name="ContentGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="ThumbCol" Width="0"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition x:Name="FavoritesCol" Width="0"/>
        </Grid.ColumnDefinitions>

        <!-- Panel lateral de miniaturas (sin ScrollViewer externo para mantener virtualización) -->
        <Border x:Name="ThumbPanel" Grid.Column="0" Width="240" Background="{DynamicResource PanelBackgroundBrush}" Visibility="Collapsed">
            <ListBox x:Name="ThumbList"
                     BorderThickness="0"
                     Background="Transparent"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.CanContentScroll="True"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     SelectionChanged="ThumbList_SelectionChanged">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="8" Background="{DynamicResource CardBackgroundBrush}" CornerRadius="6" Padding="6" Style="{DynamicResource ThumbCardStyle}">
                            <Border.ToolTip>
                                <ToolTip Placement="Right" StaysOpen="False">
                                    <Image Source="{Binding Thumbnail}" Width="280" Height="380" Stretch="Uniform" />
                                </ToolTip>
                            </Border.ToolTip>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Image Source="{Binding Thumbnail}" Stretch="Uniform" Height="140"/>
                                <TextBlock Grid.Row="1" Text="{Binding PageNumber}" Foreground="{DynamicResource SecondaryTextBrush}" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Área de contenido -->
        <ContentControl Grid.Column="1" x:Name="MainContentArea" Content="{Binding CurrentView}"
                        ClipToBounds="True"/>

        <!-- Panel de Favoritos (colapsado por defecto) -->
        <Border Grid.Column="2" x:Name="FavoritesPanel" Background="{DynamicResource PanelBackgroundBrush}" Visibility="Collapsed" Width="320">
            <local:FavoritesSection x:Name="FavoritesSection" />
        </Border>
    </Grid>

        
    </Grid>
</Window>